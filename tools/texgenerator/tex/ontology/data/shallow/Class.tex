\tree{Class}{the concept of declaring and invoking classes in [[OO programming]]}{
\tab\instance{Abstract class}{a [[:Category:class]] that cannot be directly instantiated but subclasses may be amenable to instantiation}
\tab\instance{Concrete class}{a [[:Category:class]] that can be directly instantiated as opposed to an [[abstract class]]}
}