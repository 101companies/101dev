Requirements:
 - The "serializable" project is required.
   > SVN : http://developers.svn.sourceforge.net/svnroot/developers/repository/101companies/code/serializable/

A minimalistic GUI for companies is provided.
The GUI provides the following gestures:
- Navigate hierarchical structure of departmental nesting.
- Edit all fields of types string and number.

The project uses a poormen's MVC architecture.
Project "serializable" provides the model for the present project.
We do not assume any change-notification services on the model.
The GUI 