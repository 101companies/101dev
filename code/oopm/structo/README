// (C) 2009 Ralf Laemmel

An implementation of a tiny imperative language.
The name, Structo, refers to structogram.

The package hierarchy works as follows:

* structo
  * samples: samples, test input, baseline output
  * types: different object models for Structo
    * pure: POJOs; no behavior
    * virtual: object model with interpreter methods
    * visitor: visitor-enabled object model
  * processor: ANTLR-based language processors
    * acceptor: just an acceptor; no processing
    * virtual: parser + interpreter
    * visitor: parser + checker + interpreter
